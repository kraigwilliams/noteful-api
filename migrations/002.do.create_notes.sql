CREATE TABLE notes(
      id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      note_name TEXT NOT NULL,
      folderId INTEGER REFERENCES folders(id) NOT NULL
      modified TIMESTAMP DEFAUT NOW(),
      content TEXT NOT NULL

);